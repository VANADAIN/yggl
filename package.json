{
	"name": "yggl-monorepo",
	"private": true,
	"version": "0.0.0",
	"engines": {
		"node": ">=22.13",
		"pnpm": ">=11"
	},
	"scripts": {
		"download": "node scripts/download-binaries.mjs",
		"build": "pnpm -r build",
		"test": "pnpm -r test",
		"test:coverage": "pnpm --filter yggl test:coverage",
		"test:coverage:integration": "pnpm --filter yggl test:coverage:integration",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"prepare": "husky"
	},
	"lint-staged": {
		"*.{ts,js,mjs,json}": "biome check --write --no-errors-on-unmatched"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.15",
		"@vitest/coverage-v8": "^3.1.4",
		"husky": "^9.1.7",
		"lint-staged": "^17.0.5",
		"tsup": "^8.4.0",
		"typescript": "^5.8.3",
		"vitest": "^3.1.4"
	}
}
